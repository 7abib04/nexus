<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.5</version>
        <relativePath/>
    </parent>

    <groupId>com.buy01</groupId>
    <artifactId>backend</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>backend</name>
    <description>E-commerce microservices backend skeleton</description>

    <modules>
        <module>discovery-service</module>
        <module>gateway-service</module>
        <module>user-service</module>
        <module>product-service</module>
        <module>media-service</module>
        <module>order-service</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <spring-cloud.version>2023.0.3</spring-cloud.version>
        <jacoco.version>0.8.12</jacoco.version>

        <!-- SonarQube (see ../sonarqube/README.md). sonar.host.url/sonar.token are
             passed on the command line by CI and the Jenkins pipeline; they're
             intentionally not defaulted here since the analysis target differs
             between the ephemeral CI SonarQube and the persistent local one. -->
        <sonar.projectKey>buy-01-backend</sonar.projectKey>
        <sonar.projectName>Buy-01 Backend</sonar.projectName>
        <sonar.exclusions>**/target/**</sonar.exclusions>

        <!-- Nexus Repository Manager (see ../nexus/README.md). Overridden with
             -Dnexus.url=http://nexus:8081 by the Jenkins pipeline, which runs
             inside a container on the shared buy01-nexusnet Docker network
             instead of the host's published localhost:8085 port. -->
        <nexus.url>http://localhost:8085</nexus.url>
    </properties>

    <distributionManagement>
        <repository>
            <id>nexus-releases</id>
            <url>${nexus.url}/repository/maven-releases/</url>
        </repository>
        <snapshotRepository>
            <id>nexus-snapshots</id>
            <url>${nexus.url}/repository/maven-snapshots/</url>
        </snapshotRepository>
    </distributionManagement>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <!-- Inherited by every module: emits target/site/jacoco/jacoco.xml per
                 service, which the sonar-maven-plugin reads by convention and which
                 sonar.coverage.jacoco.xmlReportPaths could also point at explicitly
                 if the report location ever moves. -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
